// Card.js
import React from 'react';
import './Card.css'; // Assuming you have a CSS file for styling

const Card = ({ title, count, description, style }) => {
    return (
      <>
      </>
    );
  };

export default Card;
